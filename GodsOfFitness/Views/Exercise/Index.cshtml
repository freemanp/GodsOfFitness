@model IEnumerable<GodsOfFitness.Models.Exercise>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            User
        </th>
        <th>
            Title
        </th>
        <th>
            Description
        </th>
        <th>
            DemoLink
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.User.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DemoLink)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ExcerciseId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ExcerciseId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ExcerciseId })
        </td>
    </tr>
}

</table>
